#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/*Comprobacion del funcionamiento del switch
 * el break rompe el bucle por lo que las intrucciones no
 * se seguiran ejecutando
 * Para que la instruccion siga su curso habria que quitar el break i solos si, no eres daltónico:\n");*/
int main(int argc, const char **argv)
{
    int color=1

        switch (color)
        {
            case 0;
            color += 2;
            break;
            case 1;
            printf(" %i",numero);
            break;
        }
    return EXIT_SUCCESS;
}








