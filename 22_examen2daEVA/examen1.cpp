/*crear un programa que determine cuando un numero es perfecto y mostr
 * ar en una matriz los primeros 10*/
#include <stdio.h>
#include <stdlib.h>

int main(){

    int perfecto[N];
    int cadan_numero=1;
    int encontrado=0;
    while(encontrado<5)

        if(es_perfecto(cada_numero++))
            perfecto[encontrado++]=cada_numero;


    return EXIT_SUCCESS;

}
